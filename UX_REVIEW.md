# 猫吃了吗 — UX Review

> 状态：**进行中**（agent 视角 + 真人走查合一版）
> 日期：2026-08-07
> 作者：Mavis（agent 视角）+ lulu（真人走查）
> 跑过的版本：HEAD = `d0607fc`，iPhone 14 viewport 模拟 + Safari PWA 真机
> 截图目录：`output/playwright/`

---

## TL;DR

**产品有清晰的灵魂**——挑食猫主人记录试吃、补货少走弯路，4 分组卡片是杀手锏。
但当前版本（HEAD `d0607fc`）在「真机体验」和「结构合理性」上有不少问题：
- 通用层：自适应缺失、顶部栏不吸顶、关键操作靠文字按钮
- 录入层：字段拆分反人性、拍照流程不顺畅、保存后回首页路径太长
- 补货清单页：信息密度高但组织混乱（昵称/按钮/搜索/tab 都有问题）
- 状态机：只加照片就被默认归到"少量再试"，逻辑需要重审

**整体判断：「有想法的 v0.5」，需要一轮系统打磨。**

---

## 来源说明

| 来源 | 内容 | 占比 |
|---|---|---|
| agent 视角（旧 review + 代码审阅） | 8 张当前版/老版截图对比、`preview.js` 业务规则、首页/详情/补货清单流程分析 | 约 40% |
| 真人走查（lulu 4 轮 30+ 条） | iPhone 真机 + Safari PWA 体验记录、UI 不满、流程断层 | 约 60% |

**agent 视角回答"结构上对不对"，真人走查回答"用起来怎么样"**。这一版以真人走查为主、agent review 为参照。

---

## 优先级速查（30 条问题）

| 优先级 | 数量 | 主题 |
|---|---|---|
| **P0 必修** | 6 | 通用层（自适应/吸顶/通用图标）+ 拍照后回到首页的路径 + "最近吃过"商品去重 + 录入结构反思 |
| **P1 重要** | 11 | 补货清单页组织（昵称/按钮/搜索）、首页/详情头部、tab 选中态、记录新品页结构化 |
| **P2 优化** | 9 | 文案/命名/视觉细节、二级筛选、tab 视觉 |
| **P3 锦上添花** | 4 | 拍照素材预览、暗色模式、可访问性增强 |

---

## 全站性问题（影响所有页面）

| 页面 | 场景 | 问题 | 影响 | 优化建议 | 优先级 | 当前状态 |
|---|---|---|---|---|---|---|
| 全站 | 真机使用 | **页面缺少自适应方案**：设计基准 390×844，设备变大时未按比例缩放 | iPad / 大屏手机 / 横屏体验崩坏，留白/拥挤 | 引入 `transform: scale()` 或 `vw/vh` 比例缩放容器；至少保证大屏下卡片/字号不拉伸变形 | P0 | ❌ 未实现 |
| 全站 | Safari 桌面 Web App | **每个页面的顶部背景图缺失** | PWA 模式下顶部完全裸奔，破坏沉浸感 | 检查 PWA 模式下 `lime-dots.png` 等背景图为何不渲染（可能是 `viewport-fit=cover` + safe-area 配合问题） | P0 | ❌ 未实现 |
| 全站 | 全页面滚动 | **顶部栏不吸顶**：滚动时 topbar 一起滚走 | 长内容场景下用户必须滚回顶部才能操作返回/标题 | `.topbar` 加 `position: sticky; top: 0; z-index: 10` + 半透明背景模糊 | P0 | ❌ 未实现 |
| 全站 | 任意页面交互 | **返回/关闭/刷新等基础操作是文字按钮，不是图标** | 视觉权重不统一、占用空间、缺乏语义 | 统一图标系统：chevron-left、x、refresh，并提供 outlined / filled 两套 | P0 | ❌ 未实现（`add.wxml:3`、`detail.wxml:3` 仍是"返回"文字） |
| 全站 | 底部导航 | **tab 图标选中时会抖动**：怀疑选中和未选中是两张不同尺寸/不同描边的图 | 切换 tab 时位移明显，体验粗糙 | 选中态用同图 + 颜色 + 轻微 scale 动效，不要换图 | P1 | ❌ 未实现（`bottom-nav/index.wxml` 中 selected 用 class，未用图标切换） |

---

## 首页（`pages/home`）

| 页面 | 场景 | 问题 | 影响 | 优化建议 | 优先级 | 当前状态 |
|---|---|---|---|---|---|---|
| 首页 | 浏览历史 | **"最近尝过"会重复展示同款商品**：lulu 反馈"同一个商品吃 2 次出现 2 次" | 浪费首屏位置、误导用户觉得"已经吃很多" | 1) 按 `food.id` 去重展示；2) 改为"现有食物列表"，点击直接进入记录反馈；3) "记录"入口（底部 tab）只做新增产品 | P0 | ❌ 未实现（`home.js:31` 直接 `foods.slice(0, 4)`，不去重） |
| 首页 | 首屏 | 文案"Hi 噜噜 这次吃的怎么样？"是问句但首页没给反馈入口 | 认知断点（agent review 旧问题） | 在 quick-strip 上加"📝 记录这次表现"快捷卡 | P1 | 🟡 部分（quick-strip 有"补反馈"卡，但只能跳到第一只 trial 食物） |
| 首页 | 首屏 | "挑食档案" pill 信息密度低、含义模糊 | 首屏占用空间但没说清什么 | 改为动态数据 pill：「今天 0 款 / 本周 3 款」 | P2 | ❌ 未实现 |
| 首页 | 标签 | "埋屎"作为分类标签对分享/截图场景不雅（agent 旧问题） | 影响社交分享 | 短期：保留但加 tooltip 解释；长期：改为"拒绝/不喜欢" | P2 | ❌ 未实现（`library.js:34` 仍用"埋屎避雷"） |
| 首页 | 空状态 | "先看看示例"按钮位置突兀（agent 旧问题） | 新用户困惑 | 移到空状态卡内部，不要常驻在 hero 区 | P2 | 🟡 已改为"先看看示例"（旧 review P1），但位置仍突兀 |

---

## 记录新品页（`pages/add`）

| 页面 | 场景 | 问题 | 影响 | 优化建议 | 优先级 | 当前状态 |
|---|---|---|---|---|---|---|
| 新品 | 表单录入 | **结构化字段反人性**：品牌+系列+肉+克数拆开记，对真实商品名（如"GrandaPet 菲力系列纯鸡肉 85g"）不自然 | 用户录入累、对长商品名易放弃 | 改为"商品名一栏 + 自由标签"：1 栏主输入框承担识别，其余字段作为可选 tag | P0 | ❌ 未实现（`add.wxml:36-95` 仍是 5 字段上下结构） |
| 新品 | 字段填写 | **"系列"不是通用字段**：有的商品没系列、有的是 SKU 编号 | 用户在"系列"框卡住 | 移除"系列"独立字段，合并到"商品名/规格"一栏 | P0 | ❌ 未实现（`add.wxml:49` 仍是"系列或名称"） |
| 新品 | 字段排版 | **字段上下结构，浪费空间**：label 在上、input 在下 | 长字段（如多行）滚动成本高 | 改为左右结构：左 label、右 value，节省纵向空间 | P1 | ❌ 未实现（`add.wxss:140-167` 是 flex column） |
| 新品 | 字段排版 | **"食物类型"独立大块、占位重** | 视觉重心错位 | 1) 改成横向滑动 tab（不换行）；2) 改为左右结构后并入主字段列表 | P1 | ⚠️ 食物类型在数据层有（`foodType`），但 add 表单没暴露选择器 |
| 新品 | 拍照 | **拍照只支持实时拍摄，不能从相册预先选** | 紧急场景（猫已经吃了）不能快速补录 | 入口改用 `wx.chooseMedia`，让用户选相机/相册（**注：`add.js:51` 实际已传 `sourceType: ["camera", "album"]`，可能是 iOS PWA 下相册入口不显示，需真机验证**） | P1 | 🟡 代码已支持，真机行为待验 |
| 新品 | 拍照 | **缺"先拍照入库、之后补字段"工作流** | 来不及的当下无法快速记录 | 1) 拍照后允许直接"保存到清单"（字段全空也能存）；2) 在清单卡片上提示"补充信息" | P1 | 🟡 部分（`add.js:76` 已允许 photoPath + name/brand 任一非空），但 UI 没引导 |
| 新品 | 提交 | **【加入试吃清单】按钮文案太长、未吸底** | 滚动到底才看到 CTA，体验断层 | 1) 文案改"加入清单"；2) 吸底（`position: fixed; bottom`） | P1 | ❌ 未实现（`add.wxml:99` 是"加入试吃清单"，无吸底） |
| 新品 | 步骤引导 | 顶部两步"先拍下包装 / 补一点线索"分级清晰（agent 旧优点） | — | 保留 | — | ✅ 已实现 |
| 新品 | 拍照卡片 | 大色块"lime-dots"非常醒目（agent 旧优点） | — | 保留 | — | ✅ 已实现 |
| 新品 | 引导文案 | "首版先用照片记住它。下面只填你认得的部分"（agent 旧优点） | 缓解录入压力 | 保留，并升级为左右结构后的字段提示 | — | ✅ 已实现 |

---

## 食物详情页（`pages/detail`）

| 页面 | 场景 | 问题 | 影响 | 优化建议 | 优先级 | 当前状态 |
|---|---|---|---|---|---|---|
| 详情 | 头部视觉 | **头部图片变化大、识别性不高**：照片色块和大块背景色反差过大 | 用户一眼认不出"这是哪款" | 1) 减小头部高度；2) 照片用白色卡片包裹；3) 顶部状态 pill 与色块协调 | P1 | ❌ 未实现（`detail.wxml:10-14` 是大色块背景） |
| 详情 | 进度展示 | 长期回购考核进度条 + 3 个点（agent 旧优点） | 目标感强 | 保留并细化（每次反馈后短动画） | — | ✅ 已实现 |
| 详情 | 状态 banner | 5 种状态 banner 配色清晰（agent 旧优点） | — | 保留 | — | ✅ 已实现 |
| 详情 | 历史 | "主子的投票记录"只显示当前可见的历史条数 | 用户看不到完整历史 | 加"查看全部"展开；或上拉加载 | P2 | 🟡 已实现历史卡片（`detail.wxml:117-127`），但需确认是否全量 |

---

## 补货清单页（`pages/library`）

| 页面 | 场景 | 问题 | 影响 | 优化建议 | 优先级 | 当前状态 |
|---|---|---|---|---|---|---|
| 清单 | 命名 | "清单页"和"加入试吃清单"两个"清单"含义不同 | 用户认知混淆 | "清单" tab 改"补货清单"（底部 tab 已是），add 按钮文案"加入试吃清单"改"加入清单"或"开始试吃" | P1 | 🟡 tab 已是"补货清单"，add CTA 仍是"加入试吃清单" |
| 清单 | 顶部栏 | **左侧产品 logo + 产品名 + 右侧"编辑昵称"按钮很奇怪** | 信息层级错乱 | 1) 编辑入口改图标按钮（铅笔），贴在名字旁边；2) 右侧放"总款数" pill（已有 `library-count`，但与编辑按钮冲突） | P1 | ❌ 未实现（`library.wxml:3-7` 左边 logo+名字、右边 `{{total}} 款`） |
| 清单 | 编辑昵称 | **弹窗太重**：用弹窗编辑一行字，且滚动时光标会上下飘（input 没定位） | 操作过重、易误关 | 1) 改为行内编辑（点名字直接变 input）；2) 或轻量 popover，input 自带 `adjust-position` | P1 | ❌ 未实现（需查 nickname 编辑 UI） |
| 清单 | 编辑昵称 | **信息重复**：顶部标题已是昵称、输入框上方又写"昵称" | 视觉噪音 | 弹窗/input 上方不要重复 label，标题已说 | P1 | ❌ 未实现 |
| 清单 | 编辑昵称 | 按钮文案是"保存昵称" / "确定"，重复信息 | 视觉噪音 | 统一为"保存" / "完成" | P1 | ❌ 未实现 |
| 清单 | 弹窗/侧滑 | **关闭用文本（"取消"/"×"）而不是图标** | 不一致 | 关闭统一用 `×` 图标按钮 | P1 | ❌ 未实现 |
| 清单 | tab/筛选 | **tab 栏视觉很丑** | 影响整体感 | 1) tab 选中态用底部下划线 + 加粗；2) 颜色统一中性色，分组色只用在内容卡上 | P2 | ⚠️ 当前 library 页无 tab 切换；lulu 看到的 tab 可能是分组 chip 或 quick-strip 类型，待确认 |
| 清单 | 筛选 | **tab 下方二级筛选（如食物类型）字号太小** | 难点击 | 字号 28rpx+，高度 ≥ 60rpx | P2 | ❌ 未实现 |
| 清单 | 状态机 | **只加照片没写反馈的食品，会被默认归到"少量再试"分组**：lulu 反馈这是逻辑 bug | 新入库的"未观察"被当作"已观察 1 次" | 1) 区分"未投过票"和"少量再试"两个状态；2) UI 上加"未评价"分组或灰色状态 | P1 | ❌ 未实现（`library.js:76-79` 直接按 `shoppingGroup` 分组，trial 状态包含未投票） |
| 清单 | 搜索 | **搜索框聚焦时，iOS Safari 会自动放大页面** | 体验诡异、布局错位 | 1) `<input>` 字号 ≥ 16px（已满足）；2) 或在聚焦时手动控制 viewport；3) 改为图标按钮 + 点击后从右往左推入搜索框 + 自动聚焦 + 唤起键盘 | P1 | ❌ 未实现（`library.wxml:19-28` 是常驻 search box） |
| 清单 | 搜索 | "搜索" label 占用空间 | — | 去掉 label，placeholder 写"搜品牌、口味或质地"即可 | P2 | ❌ 未实现 |
| 清单 | 4 分组 | 4 分组卡片（放心买/少量再试/近期观察/埋屎避雷）是产品杀手锏（agent 旧优点） | — | 保留 | — | ✅ 已实现 |
| 清单 | 顶部 hero | 绿底 hero "先看主子投过的票"很有力（agent 旧优点） | — | 保留 | — | ✅ 已实现 |
| 清单 | FAB | 红色"拍新品" FAB（agent 旧优点） | — | 保留 | — | ⚠️ 当前是底部 tab 居中按钮，不是 FAB，但体验类似 |

---

## 反馈页（`pages/feedback`）

| 页面 | 场景 | 问题 | 影响 | 优化建议 | 优先级 | 当前状态 |
|---|---|---|---|---|---|---|
| 反馈 | 完成后导航 | **反馈完按返回，要 back 多次才能回首页**：lulu 反馈从 feedback → detail → add → home 多次 back | 流程尾部体验糟糕 | 1) 反馈完成后直接 `wx.reLaunch` 到 home；2) 或弹"完成 toast"并显示"回到首页"按钮 | P0 | ❌ 未实现（`feedback.js` 提交后需查跳转逻辑，lulu 体验确认是 back 链） |
| 反馈 | emoji 投票 | 5 个 emoji 选项 + 引导（agent 旧优点） | 降低表达门槛 | 保留 | — | ✅ 已实现 |
| 反馈 | 引导文案 | "看它的行为，不只看剩多少"是绝佳引导（agent 旧优点） | 消除"剩多少"焦虑 | 保留 | — | ✅ 已实现 |
| 反馈 | 备注 | 备注 textarea + 协助 chip 友好（agent 旧优点） | — | 保留 | — | ✅ 已实现 |

---

## 状态机 / 数据模型反思

| 场景 | 问题 | 影响 | 优化建议 | 优先级 | 当前状态 |
|---|---|---|---|---|---|
| 新入库食物 | "只加照片"和"加照片+1次反馈"被合并到同一 trial 状态 | 补货清单分组的"少量再试"包含"完全没投过票"的食品，误导 | 引入 `untriaged` 状态，区分"未投过票"和"已投 1-2 次好反馈" | P1 | ❌ 未实现（`utils/rules.js` 需查） |
| 商品重名 | "最近尝过"不去重，按 `createdAt` 倒序 | 同一商品吃 N 次就展示 N 次 | 按 `food.id` 去重，最多展示 1 条 | P0 | ❌ 未实现（`home.js:31`） |
| 同款多次反馈 | 反馈完一次后没引导"是不是同一款再投一次" | 同一商品分散在多处 | 反馈页加"是不是同一款"快速切换 | P2 | ❌ 未实现 |
| 数据导出/导入 | ADR-006 已立项 | — | — | — | 📋 下一阶段 |

---

## 主题层 / 跨页面反思

### 信息架构

- ⚠️ **首页极简 vs 核心价值深埋**：用户必须主动去"清单"tab 才看到分组（agent 旧问题）
- ⚠️ **状态命名分歧**：UI 友好（放心买/少量再试）但 share 场景不雅（埋屎）
- ⚠️ **"清单"一词多义**：add 的"加入试吃清单"和 tab 的"补货清单"是两个东西

### 文案

- ✅ **"看它的行为，不只看剩多少"** —— 绝佳引导（保留）
- ✅ **"拍下包装 照片就是最可靠的识别凭证"** —— 解释到位（保留）
- ⚠️ **"Hi 噜噜 这次吃的怎么样？"** —— 问句没响应（home）
- ⚠️ **"加入试吃清单"** —— 跟"补货清单"混（add）
- ⚠️ **"埋屎避雷"** —— 调性一致但 share 场景不雅（library）

### 视觉

- ✅ **emoji 化反馈** —— 降低门槛（保留）
- ✅ **颜色分组**（绿/薄荷/粉/紫）—— 直觉映射（保留）
- ⚠️ **首页极简冷清** —— 缺温度感（旧问题，可考虑加微动效）
- ⚠️ **占位图全是同款猫头 SVG** —— 单调（加分类占位图：罐头/冻干/干粮轮廓）

### 可访问性

- ⚠️ **未做移动端横屏适配测试**
- ⚠️ **未做字体缩放测试**
- ⚠️ **未做 VoiceOver 朗读测试**

### 性能

- ✅ **build 体积小**（dist/client 7 个文件）
- ✅ **路由用 URL query**，不引入 SPA 框架
- ⚠️ **每次操作都重 render 整个 main** —— 数据规模小时无感，几百条时需测

---

## 改进路线图

### P0 必修（影响产品能不能用、价值能不能传达到）

1. **页面自适应方案**（全站）
2. **顶部栏吸顶**（全站）
3. **通用图标系统**（返回/关闭/刷新）
4. **Safari PWA 顶部背景图修复**
5. **首页"最近尝过"按商品去重 + 改为快速记录入口**
6. **反馈完成后一键回首页**（`wx.reLaunch`）
7. **录入字段反思**：移除"系列"独立字段，改为"商品名 + 自由 tag"

### P1 重要（影响日常使用体验）

8. **补货清单页组织**：编辑昵称改行内 / 关闭用图标 / 按钮文案简化
9. **记录新品 CTA**：改"加入清单" + 吸底
10. **记录新品拍照流程**：相册入口明示 + "先拍照入库"工作流
11. **记录新品字段左右结构 + 食物类型横向 tab**
12. **状态机**：`untriaged` 区分"未投过票"
13. **搜索框**：聚焦防放大 + 改图标推入式
14. **tab 选中态**：同图 + 颜色 + 动效，不换图
15. **详情页头部视觉重做**
16. **首页"挑食档案" pill 动态化**

### P2 优化

17. **录入页"先拍照入库"UI 引导**
18. **录入页拍照前后切换动画**
19. **补货清单 tab / 二级筛选视觉重做**
20. **补货清单"搜索"label 去掉**
21. **详情页完整反馈历史**
22. **占位图分类化**（罐头/冻干/干粮轮廓）
23. **首页加吉祥物元素**（恢复老版兔子）
24. **埋屎标签委婉版**（主用于 share 场景）
25. **空状态"先看看示例"位置优化**

### P3 锦上添花

26. **PWA 安装提示优化**（iOS 添加到主屏幕）
27. **离线/在线状态指示**
28. **暗色模式**
29. **数据导出/导入**（ADR-006 独立阶段）
30. **横屏适配 / 字体缩放 / VoiceOver 测试**

---

## 待办与下一步

- [ ] **lulu 真机二次验证**：iPhone 上跑一遍 P0 清单的修复
- [ ] **agent 跑剩余截图**：用 playwright 验证修复后视觉
- [ ] **状态机讨论**：`untriaged` 是否需要？影响 `rules.js` 和 `library.js`
- [ ] **录入结构重设计**：要不要从"结构化字段"切到"商品名 + 自由 tag"？影响 `add.wxml` 和 `data-store.js` 的 food 字段语义
- [ ] **P0 改进项开 ticket**

---

## 截图清单

### 当前版（HEAD `d0607fc`）
- `output/playwright/01-home.png` —— 首页空状态
- `output/playwright/02-home-with-sample.png` —— 首页载入示例后
- `output/playwright/03-add.png` —— 添加流程（待补）
- `output/playwright/04-library.png` —— 补货清单（待补）
- `output/playwright/05-detail.png` —— 详情页（待补）
- `output/playwright/06-feedback.png` —— 反馈页（待补）

### 老版（Jul 27 设计稿）
- `output/playwright/home.png` —— 完整设计版首页
- `output/playwright/add.png` —— 步骤化添加
- `output/playwright/feedback.png` —— emoji 反馈
- `output/playwright/library.png` —— 4 分组清单
- `output/playwright/detail.png` —— 进度条详情

---

## 备注

- 这份 review 整合了之前 agent 视角的 UX_REVIEW（2026-08-07 早版）与 lulu 4 轮真人走查
- "当前状态"列基于阅读 HEAD `d0607fc` 源码判断，如有出入以真机/代码为准
- 优先级 P0/P1/P2/P3 由"影响范围 × 修复成本"综合判断
- 不改代码，只列问题和建议

*这份 review 是 agent + 真人合一的快照，会随修复进度持续更新。*
